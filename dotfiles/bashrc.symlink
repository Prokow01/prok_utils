# Sourcing the aliases file, since this isn't automatic in OSX


# Set CLICOLOR if you want Ansi Colors in iTerm2 
export CLICOLOR=1

# Set colors to match iTerm2 Terminal Colors
export TERM=xterm-256color


# Script for checking if commands exist more efficent than using which

#export PATH="/Users/${IAM}/anaconda3/bin:$PATH";
export one_drive=$HOME/OneDrive\ -\ Villanova\ University

export MY_BOX="parallels@10.211.55.5";
